# Explicit static routes first (Cloudflare Pages: files win over redirects, but
# keep intent clear). Do NOT SPA-fallback everything to index.html — that made
# /blog /projects /research look like the homepage to bots and users.

/home    /   301
/home/   /   301

# Legacy filenames
/cognitive-os  /cognitive-os.html  301
