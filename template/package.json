{
  "name": "aegntic-site-template",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A configurable, dependency-free edition of the aegntic.ai homepage.",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node build.mjs",
    "preview": "node serve.mjs",
    "sync": "node sync.mjs",
    "check": "node check.mjs",
    "example": "node build.mjs --config examples/studio.mjs --out dist-studio"
  }
}
